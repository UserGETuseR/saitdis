import { createServer, type IncomingMessage, type ServerResponse } from 'node:http';
import { createHash, randomBytes, timingSafeEqual } from 'node:crypto';
import { readFile } from 'node:fs/promises';
import { extname, join, normalize, resolve } from 'node:path';
import { PrismaClient } from '@prisma/client';

const required = (name: string) => { const value = process.env[name]?.trim(); if (!value) throw new Error(`${name} is required.`); return value; };
const config = { databaseUrl: required('DATABASE_URL'), botToken: required('TELEGRAM_BOT_TOKEN'), webhookSecret: required('TELEGRAM_WEBHOOK_SECRET'), botUsername: required('TELEGRAM_BOT_USERNAME').replace('@', ''), publicUrl: required('PUBLIC_URL').replace(/\/$/, ''), adminSetupSecret: required('VETSVET_ADMIN_SETUP_SECRET'), organizationId: process.env.VETSVET_ORGANIZATION_ID?.trim() || 'vetsvet-production', sbpPhone: required('VETSVET_SBP_PHONE'), port: Number(process.env.PORT ?? 4400) };
const db = new PrismaClient({ datasources: { db: { url: config.databaseUrl } } });
const root = process.cwd(); const publicRoot = resolve(root, 'apps', 'public-web'); const clientRoot = resolve(root, 'apps', 'client-web'); const authRoot = resolve(root, 'apps', 'auth-web'); const photoRoot = resolve(root, 'Photo');
const digest = (value: string) => createHash('sha256').update(value).digest('hex');
const sameSecret = (left: string, right: string) => { const a = Buffer.from(left), b = Buffer.from(right); return a.length === b.length && timingSafeEqual(a, b); };
const mime: Record<string, string> = { '.html':'text/html; charset=utf-8','.js':'text/javascript; charset=utf-8','.css':'text/css; charset=utf-8','.png':'image/png','.jpg':'image/jpeg','.jpeg':'image/jpeg','.svg':'image/svg+xml' };
type TgUpdate = { update_id:number; message?:{message_id:number;chat:{id:number};from?:{id:number;first_name?:string;last_name?:string};text?:string;photo?:unknown[]}; callback_query?:{id:string;data?:string;from:{id:number};message?:{chat:{id:number}}} };

async function body(request: IncomingMessage) { const chunks: Buffer[]=[]; for await(const part of request)chunks.push(Buffer.isBuffer(part)?part:Buffer.from(part)); return Buffer.concat(chunks).toString('utf8'); }
function json(response: ServerResponse,status:number,payload:unknown){response.writeHead(status,{'content-type':'application/json; charset=utf-8','cache-control':'no-store'});response.end(JSON.stringify(payload));}
function cookie(request:IncomingMessage,name:string){return request.headers.cookie?.split(';').map(v=>v.trim()).find(v=>v.startsWith(`${name}=`))?.slice(name.length+1);}
async function telegram(method:string,payload:Record<string,unknown>){const result=await fetch(`https://api.telegram.org/bot${config.botToken}/${method}`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(payload)});if(!result.ok)throw new Error(`Telegram ${method} failed.`);}
async function say(chatId:string,text:string,keyboard?:unknown){await telegram('sendMessage',{chat_id:chatId,text,reply_markup:keyboard?{inline_keyboard:keyboard}:undefined});}
async function ensureOrganization(){await db.organization.upsert({where:{id:config.organizationId},update:{},create:{id:config.organizationId,legalName:'VetSvet',displayName:'ВетСвет'}});}
async function ownerFor(telegramUserId:string,fullName:string){return db.owner.upsert({where:{organizationId_telegramUserId:{organizationId:config.organizationId,telegramUserId}},update:{fullName},create:{organizationId:config.organizationId,telegramUserId,fullName}});}
async function session(request:IncomingMessage){const token=cookie(request,'vetsvet_session');if(!token)return undefined;return db.authSession.findFirst({where:{tokenHash:digest(token),state:'ACTIVE',expiresAt:{gt:new Date()}},include:{user:true}});}
async function serve(response:ServerResponse, base:string,path:string){const target=normalize(join(base,path));if(!target.startsWith(base)){json(response,403,{error:'FORBIDDEN'});return;}try{const data=await readFile(target);response.writeHead(200,{'content-type':mime[extname(target)]??'application/octet-stream','cache-control':extname(target)==='.html'?'no-store':'public, max-age=300'});response.end(data);}catch{json(response,404,{error:'NOT_FOUND'});}}

async function handleUpdate(update:TgUpdate){const message=update.message;const callback=update.callback_query;
  if(callback?.message?.chat && callback.data){const m=callback.data.match(/^payment:(approve|reject):([0-9a-f-]{36})$/i);if(!m)return;const admin=await db.telegramAdminChat.findFirst({where:{chatId:String(callback.message.chat.id),telegramUserId:String(callback.from.id)}});if(!admin){await telegram('answerCallbackQuery',{callback_query_id:callback.id,text:'Нет прав администратора.',show_alert:true});return;}const proof=await db.telegramPaymentProof.findUnique({where:{id:m[2]}});if(!proof||proof.state!=='PENDING_REVIEW'){await telegram('answerCallbackQuery',{callback_query_id:callback.id,text:'Чек уже обработан.'});return;}const approved=m[1]==='approve';await db.$transaction([db.telegramPaymentProof.update({where:{id:proof.id},data:{state:approved?'CONFIRMED':'REJECTED',reviewedAt:new Date(),reviewedByChatId:admin.chatId}}),...(proof.requestId?[db.telegramRequest.update({where:{id:proof.requestId},data:{state:approved?'READY':'WAITING_PAYMENT'}})]:[])]);await telegram('answerCallbackQuery',{callback_query_id:callback.id,text:approved?'Оплата подтверждена':'Оплата отклонена'});await say(proof.chatId,approved?'Оплата подтверждена. Мы получили запрос и вернёмся с ответом.':'Чек пока не удалось подтвердить. Проверьте перевод и отправьте новый скриншот.');return;}
  if(!message?.from)return;const tg=String(message.from.id),chat=String(message.chat.id),text=message.text?.trim()??'',name=[message.from.first_name,message.from.last_name].filter(Boolean).join(' ')||'Владелец питомца';
  const login=text.match(/^\/start\s+login_([0-9a-f-]{36})_([A-Za-z0-9_-]{16,})$/i);if(login){const record=await db.telegramLoginRequest.findUnique({where:{id:login[1]}});if(!record||record.state!=='PENDING'||record.expiresAt<=new Date()||!timingSafeEqual(Buffer.from(record.tokenHash,'hex'),Buffer.from(digest(login[2]),'hex'))){await say(chat,'Эта ссылка недействительна или уже использована. Вернитесь на сайт и создайте новую.');return;}await db.telegramLoginRequest.update({where:{id:record.id},data:{state:'CONFIRMED',telegramUserId:tg,chatId:chat,confirmedAt:new Date()}});await db.userIdentity.upsert({where:{telegramUserId:tg},update:{},create:{telegramUserId:tg}});await ownerFor(tg,name);await say(chat,'Вход подтверждён. Вернитесь на сайт VetSvet — кабинет откроется автоматически.');return;}
  const claim=text.match(/^\/admin\s+(.+)$/);if(claim&&sameSecret(claim[1],config.adminSetupSecret)){await db.telegramAdminChat.upsert({where:{singletonKey:'primary'},update:{chatId:chat,telegramUserId:tg},create:{singletonKey:'primary',chatId:chat,telegramUserId:tg}});await say(chat,'Администратор VetSvet подключён. Сюда будут приходить заявки и чеки.');return;}
  if(text==='/start'||text==='/menu'){await say(chat,'VetSvet — забота рядом.\n\n/booking дата и пожелание — записаться\n/consultation вопрос — консультация\n/payment — как оплатить\n/emergency — срочная помощь');return;}
  if(text.startsWith('/booking')){const request=await db.telegramRequest.create({data:{telegramUserId:tg,chatId:chat,kind:'APPOINTMENT',message:text.slice(8).trim()||'Хочу записаться',state:'NEW'}});const admin=await db.telegramAdminChat.findFirst({where:{singletonKey:'primary'}});if(admin)await say(admin.chatId,`Новая заявка на запись #${request.id.slice(0,8)}\n${request.message}`);await say(chat,'Заявка на запись принята. Команда уточнит свободное время и подтвердит её здесь.');return;}
  if(text.startsWith('/consultation')){const request=await db.telegramRequest.create({data:{telegramUserId:tg,chatId:chat,kind:'CONSULTATION',message:text.slice(13).trim()||'Нужна консультация',state:'WAITING_PAYMENT'}});await say(chat,`Запрос консультации #${request.id.slice(0,8)} создан.\n\nПереведите сумму, согласованную с клиникой, по СБП на номер ${config.sbpPhone}. Затем пришлите сюда скриншот чека — его подтвердит администратор.`);return;}
  if(text==='/payment'){await say(chat,`Оплата консультации: перевод по СБП на номер ${config.sbpPhone}. После перевода отправьте в этот чат скриншот чека. Подтверждение выполняет администратор.`);return;}
  if(text==='/emergency'){await say(chat,'Если питомцу плохо прямо сейчас, не ждите ответа в чате: позвоните в клинику или обратитесь в ближайшую круглосуточную ветеринарную помощь.');return;}
  if(message.photo?.length){const request=await db.telegramRequest.findFirst({where:{telegramUserId:tg,state:'WAITING_PAYMENT'},orderBy:{createdAt:'desc'}});const proof=await db.telegramPaymentProof.create({data:{requestId:request?.id,telegramUserId:tg,chatId:chat,sourceMessageId:message.message_id,purpose:request?'CONSULTATION':'APPOINTMENT',state:'PENDING_REVIEW'}});const admin=await db.telegramAdminChat.findFirst({where:{singletonKey:'primary'}});if(admin){await telegram('forwardMessage',{chat_id:admin.chatId,from_chat_id:chat,message_id:message.message_id});await say(admin.chatId,`Чек #${proof.id.slice(0,8)} — подтвердить перевод?`,[[{text:'✓ Подтвердить',callback_data:`payment:approve:${proof.id}`},{text:'✕ Отклонить',callback_data:`payment:reject:${proof.id}`}]]);}await say(chat,'Чек получен и отправлен администратору на проверку.');return;}
}

const server=createServer(async(request,response)=>{const url=new URL(request.url??'/',config.publicUrl);try{if(request.method==='GET'&&url.pathname==='/healthz'){await db.$queryRawUnsafe('SELECT 1');json(response,200,{status:'ok'});return;}if(request.method==='POST'&&url.pathname==='/telegram/webhook'){if(request.headers['x-telegram-bot-api-secret-token']!==config.webhookSecret){json(response,403,{error:'FORBIDDEN'});return;}await handleUpdate(JSON.parse(await body(request)) as TgUpdate);json(response,200,{ok:true});return;}if(request.method==='POST'&&url.pathname==='/api/auth/telegram/start'){const secret=randomBytes(24).toString('base64url'),expiresAt=new Date(Date.now()+600000),record=await db.telegramLoginRequest.create({data:{tokenHash:digest(secret),expiresAt}});json(response,201,{requestId:record.id,expiresAt,telegramUrl:`https://t.me/${config.botUsername}?start=login_${record.id}_${secret}`});return;}if(request.method==='GET'&&url.pathname==='/api/auth/telegram/status'){const id=url.searchParams.get('requestId')??'',record=await db.telegramLoginRequest.findUnique({where:{id}});if(!record||record.expiresAt<=new Date()){json(response,404,{error:'NOT_FOUND'});return;}if(record.state!=='CONFIRMED'||!record.telegramUserId){json(response,200,{state:record.state});return;}const user=await db.userIdentity.upsert({where:{telegramUserId:record.telegramUserId},update:{},create:{telegramUserId:record.telegramUserId}});const token=randomBytes(32).toString('base64url'),expiresAt=new Date(Date.now()+30*86400000);await db.$transaction([db.authSession.create({data:{userId:user.id,tokenHash:digest(token),state:'ACTIVE',expiresAt}}),db.telegramLoginRequest.update({where:{id},data:{state:'CONSUMED',consumedAt:new Date()}})]);response.setHeader('set-cookie',`vetsvet_session=${token}; Path=/; HttpOnly; Secure; SameSite=Lax; Max-Age=2592000`);json(response,200,{state:'AUTHENTICATED',redirectTo:'/client/'});return;}if(request.method==='GET'&&url.pathname==='/api/v1/auth/me'){const s=await session(request);if(!s){json(response,401,{error:'UNAUTHORIZED'});return;}const owner=await db.owner.findFirst({where:{organizationId:config.organizationId,telegramUserId:s.user.telegramUserId??undefined}});json(response,200,{account:{mode:'CLIENT',userId:s.userId,organizationId:config.organizationId,owner}});return;}const dashboard=url.pathname.match(/^\/api\/v1\/client\/owners\/([^/]+)\/dashboard$/);if(request.method==='GET'&&dashboard){const s=await session(request);if(!s){json(response,401,{error:'UNAUTHORIZED'});return;}const owner=await db.owner.findFirst({where:{organizationId:config.organizationId,telegramUserId:s.user.telegramUserId??undefined}});if(!owner||owner.id!==decodeURIComponent(dashboard[1])){json(response,403,{error:'FORBIDDEN'});return;}const pets=await db.ownerPetRelation.findMany({where:{organizationId:config.organizationId,ownerId:owner.id},include:{pet:true}});json(response,200,{owner:{id:owner.id,fullName:owner.fullName},pets:pets.map(item=>({id:item.pet.id,name:item.pet.name,species:item.pet.species,appointments:[],careTasks:[],timeline:[]}))});return;}if(request.method==='POST'&&url.pathname==='/api/v1/auth/sign-out'){const s=await session(request);if(s)await db.authSession.update({where:{id:s.id},data:{state:'REVOKED',revokedAt:new Date()}});response.setHeader('set-cookie','vetsvet_session=; Path=/; HttpOnly; Secure; SameSite=Lax; Max-Age=0');json(response,200,{ok:true});return;}if(request.method==='GET'&&(url.pathname==='/client/'||url.pathname==='/client')){if(!await session(request)){response.writeHead(302,{location:'/auth/'});response.end();return;}await serve(response,clientRoot,'index.html');return;}if(request.method==='GET'&&url.pathname.startsWith('/client/')){await serve(response,clientRoot,decodeURIComponent(url.pathname.slice(8)));return;}if(request.method==='GET'&&(url.pathname==='/auth/'||url.pathname==='/auth')){await serve(response,authRoot,'telegram.html');return;}if(request.method==='GET'&&url.pathname.startsWith('/Photo/')){await serve(response,photoRoot,decodeURIComponent(url.pathname.slice(7)));return;}if(request.method==='GET'&&(url.pathname==='/'||url.pathname==='/index.html')){await serve(response,publicRoot,'index.html');return;}json(response,404,{error:'NOT_FOUND'});}catch(error){console.error(error);json(response,500,{error:'INTERNAL_ERROR'});}});
ensureOrganization().then(()=>server.listen(config.port,'127.0.0.1',()=>console.log(`VetSvet production server on ${config.port}`))).catch(error=>{console.error(error);process.exit(1);});
